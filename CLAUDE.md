@AGENTS.md

# Git workflow

Always work on and push to the `dev` branch. Never commit or push directly to `main`.

When the user asks to "ship", "release", or "merge to production", merge `dev` into `main` and push — but always confirm before doing so.
